# 铁路工时管家 产品需求文档（PRD）

> 版本：v5.3 | 更新日期：2026-08-18
> 纯本地化运行，数据不联网不上传

---

## 一、产品概述

### 1.1 产品名称
铁路工时管家

### 1.2 产品定位
面向铁路乘务人员（司机/副司机）的个人专属工时记录与薪酬核算工具，纯本地化运行，所有数据存储在设备本地，无任何网络请求。

### 1.3 目标用户
铁路司机、副司机等乘务人员。

### 1.4 核心价值
- 快速记录出勤/退勤工时，支持扫码自动录入
- 管理每趟乘务费用，车次与工时记录关联
- 按月统计超劳工时及超劳工资（176小时起算）
- 月度工时进度追踪（166小时标准）
- 值乘区段与标准费用可动态配置

### 1.5 交付形态
- 网页应用（单页应用，纯前端）
- 安卓 APK 安装包（支持安卓10-17，Cordova 打包）

---

## 二、技术约束

| 项目 | 要求 |
|------|------|
| 运行方式 | 纯本地化运行，无联网权限 |
| 数据存储 | 浏览器 localStorage，刷新不丢失 |
| 前端架构 | 单页应用（SPA），Hash 路由 |
| 界面语言 | 中文 |
| 安卓打包 | Cordova + Gradle，minSdk=29，targetSdk=36 |
| 竖屏锁定 | 是 |

---

## 三、页面结构（底部四 Tab 导航）

| Tab | 页面 | 说明 |
|-----|------|------|
| 首页 | 数据概览 | KPI卡片、工时进度、趋势图、最近记录 |
| 工时 | 工时记录 | 工时列表、筛选、增删改查、扫码录入 |
| 费用 | 乘务费 | 费用列表、筛选、增删改查 |
| 我的 | 个人中心 | 超劳概览、区段费用统计、区段费用标准、个人设置、薪酬参数、数据管理 |

---

## 四、功能模块

### 4.1 首页（数据概览）

#### 4.1.1 欢迎语
- 页面标题「数据概览」
- 若用户未填写个人信息，显示引导提示

#### 4.1.2 KPI 卡片（4个）
- 总工时（当前维度累计）
- 乘务费（当前维度累计）
- 司机超劳（当前维度累计）
- 副司机超劳（当前维度累计）
- 设置担当职务后，隐藏另一职务的超劳卡片

#### 4.1.3 时间维度切换
- 选项：月 / 日（已删除「周」）
- 默认：月
- 切换后 KPI、趋势图、最近记录同步刷新

#### 4.1.4 本月工时进度卡片
- 月度标准工时：**166 小时**
- 显示本月累计工时（按当前设置职务过滤）
- 进度条展示完成比例
- 文字提示：
  - 未达标：`还差 XX 小时够` / `距离月度目标还需 XX 小时`
  - 已达标：`已达成月度目标`

#### 4.1.5 工时趋势图
- 最近7天工时趋势
- 司机/副司机分色展示，含标准工时参考线

#### 4.1.6 最近记录（上下排列）
- 最近工时记录（最多5条）：日期、角色、车次、出勤-退勤、工时
- 最近费用记录（最多5条）：日期、车次、类型、角色、金额
- 上下堆叠排列，手机端可完整查看

---

### 4.2 工时记录

#### 4.2.1 工时列表
- 按日期倒序展示
- 支持按角色、客货类型、关键词（车次/区段/备注）筛选
- 支持编辑、删除

#### 4.2.2 添加工时记录（弹窗）
字段顺序及说明：

| 序号 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 1 | 日期 | 日期选择 | 默认当天，必填 |
| 2 | 客货类型 | 下拉 | 客车 / 货车 / 不填，自动填充默认值 |
| 3 | 出勤时间 | 时间选择 | 必填 |
| 4 | 车次 | 文本输入 | 输入后自动判断上/下行 |
| 5 | 退勤时间 | 时间选择 | 必填，支持跨天 |
| 6 | 上/下行 | 下拉 | 上行 / 下行 / 倒短，红色提示「倒短请手动选择」 |
| 7 | 担当角色 | 下拉 | 司机 / 副司机，自动填充默认值 |
| 8 | 值乘区段 | 下拉 | 根据客货类型+上/下行联动过滤 |
| 9 | 标准乘务费 | 文本显示 | 选择区段后自动显示该区段标准费用（绿色） |
| 10 | 备注 | 多行文本 | 选填 |

#### 4.2.3 扫码录入
- 入口：添加工时弹窗右上角「扫码录入」按钮
- 两种方式：
  - **摄像头扫码**：调用后置摄像头实时识别二维码（jsQR）
  - **相册上传**：从相册选择图片识别二维码
- 识别后自动填充：日期、出勤时间、车次、客货类型、值乘区段、备注
- 安卓端需相机和存储权限，首次使用弹出权限请求

#### 4.2.4 车次与上/下行联动规则
- 车次最后一位为**奇数** → 自动选「下行」
- 车次最后一位为**偶数** → 自动选「上行」
- 「倒短」需用户手动选择（红色提示）
- 切换上/下行时，区段下拉自动刷新

#### 4.2.5 值乘区段联动规则
区段支持**动态增删**（见 4.4.2），默认14个，分三类：

**下行区段（默认3个）：**
- 西宁-柯柯、柯柯-格尔木、西宁-格尔木

**上行区段（默认3个）：**
- 格尔木-柯柯、柯柯-西宁、格尔木-西宁

**倒短区段（默认8个）：**
- 格尔木-察尔汗-格尔木、格尔木-达布逊-格尔木、格尔木-锡铁山-格尔木、格尔木-饮马峡-格尔木
- 柯柯-德令哈-柯柯、柯柯-察汗诺-柯柯、柯柯-饮马峡-柯柯、柯柯-锡铁山-柯柯

**联动规则：**
- 客货类型=客车 → 仅显示：西宁-格尔木、格尔木-西宁
- 上/下行=下行 → 显示下行区段
- 上/下行=上行 → 显示上行区段
- 上/下行=倒短 → 显示倒短区段
- 未选择上/下行 → 显示全部区段
- 切换时若当前区段不在新列表中，自动清空

#### 4.2.6 工时计算
- 自动计算出勤到退勤的有效工时（支持跨天）
- 保存时校验：退勤时间需晚于出勤时间

---

### 4.3 乘务费

#### 4.3.1 费用列表
- 按日期倒序展示
- 支持按日期范围、费用类型、关键词筛选
- 实时汇总总金额
- 支持编辑、删除

#### 4.3.2 添加费用记录（弹窗）
字段：
- 日期（默认当天）
- 车次（下拉选择，从已有工时记录中提取去重，确保与工时关联）
- 费用类型（出乘费 / 加班费 / 夜班费 / 其他）
- 担当角色（自动填充默认值）
- 金额
- 备注

#### 4.3.3 车次关联
- 车次为下拉选择，选项自动从工时记录中提取
- 确保费用记录与实际车次对应

---

### 4.4 我的（个人中心）

#### 4.4.1 本月超劳概览
- 显示司机/副司机本月超劳时长和超劳工资
- 设置职务后隐藏另一职务
- 超劳规则：月累计工时 > 176小时 → 超劳工时 = 总工时 - 176
- 超劳工资 = 超劳工时 × 对应职务单价

#### 4.4.2 区段费用统计
- 按值乘区段汇总乘务费用
- 通过车次关联工时记录中的区段
- 显示：区段名称、记录数、费用合计
- 按费用金额降序排列
- 未关联区段的费用归为「未关联区段」

#### 4.4.3 区段费用标准（动态可配置）
- 展示所有区段及其标准乘务费用
- 每个区段可修改费用金额
- **+ 新增**按钮：输入区段名称，选择分类（上行/下行/倒短），添加后自动同步到工时录入的区段下拉
- **删**按钮：删除区段，同步从工时录入下拉移除
- **保存**按钮：保存所有费用标准
- 默认值：
  - 西宁-柯柯：60元
  - 柯柯-格尔木：80元
  - 格尔木-柯柯：80元
  - 柯柯-西宁：60元
  - 西宁-格尔木：120元
  - 格尔木-西宁：120元
  - 格尔木-察尔汗-格尔木：40元
  - 格尔木-达布逊-格尔木：45元
  - 格尔木-锡铁山-格尔木：55元
  - 格尔木-饮马峡-格尔木：65元
  - 柯柯-德令哈-柯柯：50元
  - 柯柯-察汗诺-柯柯：45元
  - 柯柯-饮马峡-柯柯：70元
  - 柯柯-锡铁山-柯柯：60元

#### 4.4.4 个人信息设置
- 姓名
- 工号
- 担当职务（司机 / 副司机 / 不设置）
- 默认客货类型（客车 / 货车 / 不设置）

#### 4.4.5 薪酬参数设置
- 标准工时/天（默认10小时）
- 月度超劳起算工时（176小时）
- 司机超劳工资单价（元/小时）
- 副司机超劳工资单价（元/小时）

#### 4.4.6 数据管理
- **导出数据**：导出 JSON 备份文件（含工时、费用、设置、区段配置）
- **导入数据**：从 JSON 备份恢复，覆盖当前所有数据
- 导入前二次确认

---

### 4.5 超劳统计（独立页面）

- 司机/副司机分栏展示
- 总工时、超劳时长、出勤天数、超劳工资
- 超劳明细列表（所有记录，按月汇总计算）
- 支持月/日维度切换

**超劳计算公式：**
```
当月总工时 ≤ 176小时 → 超劳工时 = 0
当月总工时 > 176小时 → 超劳工时 = 总工时 - 176
超劳工资 = 超劳工时 × 对应职务单价
```

---

## 五、数据模型

### 5.1 工时记录（workHour）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| date | string | 日期 YYYY-MM-DD |
| onDutyTime | string | 出勤时间 HH:mm |
| offDutyTime | string | 退勤时间 HH:mm |
| duration | number | 工时（分钟） |
| role | string | 担当角色：driver / assistant |
| trainNumber | string | 车次 |
| trainType | string | 客货类型：passenger / freight / 空 |
| direction | string | 上/下行：up / down / short / 空 |
| section | string | 值乘区段 |
| remark | string | 备注 |
| createdAt | number | 创建时间戳 |

### 5.2 乘务费（expense）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| date | string | 日期 |
| trainNumber | string | 车次（与工时记录关联） |
| type | string | 费用类型 |
| role | string | 担当角色 |
| amount | number | 金额 |
| remark | string | 备注 |
| createdAt | number | 创建时间戳 |

### 5.3 用户配置（profile）
| 字段 | 类型 | 说明 |
|------|------|------|
| name | string | 姓名 |
| employeeId | string | 工号 |
| defaultRole | string | 默认担当职务 |
| defaultTrainType | string | 默认客货类型 |

### 5.4 薪酬配置（config）
| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| standardHoursPerDay | number | 10 | 标准工时/天 |
| monthlyOvertimeThresholdHours | number | 176 | 月度超劳起算工时 |
| driverOvertimeRate | number | - | 司机超劳单价 |
| assistantOvertimeRate | number | - | 副司机超劳单价 |

### 5.5 区段列表（sectionList）
| 字段 | 类型 | 说明 |
|------|------|------|
| name | string | 区段名称 |
| cat | string | 分类：up / down / short |

### 5.6 区段费用标准（sectionRates）
- 对象，key 为区段名称，value 为费用金额（number）

---

## 六、业务规则汇总

1. **纯本地运行**：所有数据存储在 localStorage，无任何网络请求
2. **职务筛选**：设置担当职务后，首页、统计、进度只显示该职务数据
3. **月度标准工时**：166小时为满勤标准（进度展示用）
4. **超劳起算点**：月累计176小时后，超出部分计超劳
5. **超劳工资**：超劳工时 × 对应职务单价
6. **车次奇偶判断**：奇数→下行，偶数→上行，倒短需手动
7. **区段联动**：客车限2区段，上/下行/倒短各显示对应分类区段
8. **区段动态化**：可新增/删除区段，自动同步到工时录入
9. **费用车次关联**：费用车次从工时记录提取，下拉选择
10. **扫码录入**：支持摄像头和相册两种方式，自动填充工时信息

---

## 七、安卓 APK 配置

| 项目 | 值 |
|------|-----|
| 包名 | com.railway.workhours |
| 应用名 | 铁路工时管家 |
| minSdkVersion | 29（安卓10） |
| targetSdkVersion | 36（安卓16） |
| 屏幕方向 | 竖屏锁定 |
| 权限 | CAMERA、READ_EXTERNAL_STORAGE、READ_MEDIA_IMAGES |
| 打包方式 | Cordova + Gradle 8.14 + JDK17 |

---

## 八、版本迭代记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.0 | 2026-08-14 | 初始版本：工时记录、乘务费用、超劳统计、本地存储 |
| v1.1 | 2026-08-14 | 新增值乘区段字段 |
| v1.2 | 2026-08-14 | 值乘区段改为固定14项下选 |
| v1.3 | 2026-08-14 | 新增客货类型字段（客车/货车） |
| v1.4 | 2026-08-14 | 个人设置、首页欢迎语、客货区段联动 |
| v1.5 | 2026-08-14 | 保存成功提示 + 自动返回首页 |
| v1.6 | 2026-08-14 | 担当职务设置、单职务展示、超劳阈值、月度进度、超劳单价 |
| v2.0 | 2026-08-18 | 底部四Tab导航重构（首页/工时/费用/我的） |
| v2.1 | 2026-08-18 | 修复底部导航点击跳转 |
| v2.2 | 2026-08-18 | 修复所有按钮点击事件（JS语法错误） |
| v3.0 | 2026-08-18 | 扫码录入功能（摄像头+相册）、二维码自动填充 |
| v3.1 | 2026-08-18 | 彻底修复JS语法错误，所有按钮恢复正常 |
| v4.0 | 2026-08-18 | 五项修复：扫码权限、职务筛选、首页月/日维度、表单精简、区段下拉 |
| v4.1 | 2026-08-18 | 首页记录上下排列、安卓权限修复、费用车次关联工时 |
| v4.2 | 2026-08-18 | 相册上传兼容性修复、我的页面区段费用统计 |
| v5.0 | 2026-08-18 | 区段费用标准设置页面、本月工时进度卡片 |
| v5.1 | 2026-08-18 | 区段费用单列布局、表单字段重排、上/下行字段及区段联动 |
| v5.2 | 2026-08-18 | 上/下行红色提示、超劳计算改为月176小时起算 |
| v5.3 | 2026-08-18 | 区段动态增删、乘务费标题修改、工时录入自动带出标准费用 |

---

## 九、Git 仓库

- 地址：https://github.com/EA1973/railway-work-hours
- 分支：main
- 每次修改后先提交推送，再打包 APK
